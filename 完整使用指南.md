# 智能学习管理系统 - 完整使用指南

## 📋 目录

1. [项目概述](#项目概述)
2. [环境要求与检查](#环境要求与检查)
3. [数据库初始化](#数据库初始化)
4. [后端服务启动](#后端服务启动)
5. [前端服务启动](#前端服务启动)
6. [系统访问与测试](#系统访问与测试)
7. [核心功能说明](#核心功能说明)
8. [常见问题解决](#常见问题解决)

---

## 📖 项目概述

### 系统简介
智能学习管理系统是一个集成了AI分析、文档标注、智能学习路径、情绪感知等功能的现代化在线教育平台。

### 技术架构
- **后端**: Spring Boot 3.2.5 + MyBatis + MySQL 8.0
- **前端**: Vue 3 + Element Plus + Vite
- **数据库**: MySQL 8.0+ (utf8mb4字符集)
- **AI服务**: 通义千问API、讯飞API

### 主要功能模块
1. **用户管理**: 管理员、教师、学生三种角色
2. **课程管理**: 课程创建、章节管理、资源上传
3. **在线学习**: 视频学习、文档预览、学习进度跟踪
4. **文档标注**: 高亮、下划线、批注、便签等多种标注方式
5. **考试练习**: 在线考试、练习评测、AI智能分析
6. **智能学习**: 个性化学习路径、情绪感知、同伴学习
7. **数据驾驶舱**: 学习数据分析、成绩统计、行为分析

---

## 🔧 环境要求与检查

### 必需软件清单

| 软件 | 版本要求 | 下载地址 |
|------|---------|---------|
| Java JDK | 21+ | [Oracle JDK](https://www.oracle.com/java/) 或 [OpenJDK](https://openjdk.org/) |
| Maven | 3.6+ | [Apache Maven](https://maven.apache.org/) |
| MySQL | 8.0+ | [MySQL官网](https://dev.mysql.com/downloads/) |
| Node.js | 16+ | [Node.js官网](https://nodejs.org/) |
| npm | 8+ | (随Node.js安装) |

### 环境检查命令

```bash
# 检查Java版本
java -version
# 应显示: java version "21.x.x" 或更高

# 检查Maven版本
mvn -version
# 应显示: Apache Maven 3.6.x 或更高

# 检查MySQL版本
mysql --version
# 应显示: mysql Ver 8.0.x 或更高

# 检查Node.js版本
node -v
# 应显示: v16.x.x 或更高

# 检查npm版本
npm -v
# 应显示: 8.x.x 或更高
```

### 环境配置

#### MySQL配置
1. 确保MySQL服务已安装并启动
2. 创建数据库用户（如果使用root，确保密码为 `123456`）
3. 或者修改 `back/src/main/resources/application.yml` 中的数据库配置

#### Maven配置（可选）
如果下载依赖慢，可以配置国内镜像，编辑 `~/.m2/settings.xml`:
```xml
<mirrors>
  <mirror>
    <id>aliyun</id>
    <mirrorOf>central</mirrorOf>
    <url>https://maven.aliyun.com/repository/public</url>
  </mirror>
</mirrors>
```

---

## 🗄️ 数据库初始化

### 方法一：使用初始化脚本（推荐）

**Windows系统：**
```bash
# 双击运行 init_database.bat 或在命令行执行
init_database.bat
```

**Linux/Mac系统：**
```bash
# 设置执行权限并运行
chmod +x init_database.sh
./init_database.sh
```

**脚本功能特点：**
- ✅ **自动检查MySQL连接** - 启动前验证数据库连接
- ✅ **交互式选择模式** - 支持完整数据库和简化数据库两种模式
- ✅ **详细进度提示** - 显示每个步骤的执行状态（[1/7], [2/7]...）
- ✅ **错误处理** - 任何步骤失败都会停止并提示
- ✅ **自动验证** - 初始化完成后自动验证数据库
- ✅ **友好提示** - 显示测试账号和下一步操作

**初始化模式说明：**

**模式1：完整数据库（推荐用于开发测试）**
- 1个管理员账号
- 15名教师账号
- 100名学生账号
- 20门课程
- 完整的选课、考试、练习数据
- 智能学习功能数据
- 课堂重点整理数据
- 笔记标注表结构

**模式2：简化数据库（快速测试）**
- 1个管理员账号
- 1名教师账号
- 1名学生账号
- 基础表结构
- 少量测试数据

**脚本执行顺序（完整模式）：**
1. `back/src/main/resources/sql/comprehensive_database_rebuild_part1.sql` - 基础表和用户数据
2. `back/src/main/resources/sql/comprehensive_database_rebuild_part2.sql` - 100名学生数据
3. `back/src/main/resources/sql/comprehensive_database_rebuild_part3.sql` - 课程、考试、练习数据
4. `back/src/main/resources/sql/comprehensive_database_rebuild_part4.sql` - 选课、学习进度、资源数据
5. `intelligent_features_database.sql` - 智能学习功能数据
6. `class_summary_table.sql` - 课堂重点整理数据
7. `student_notes_annotations.sql` - 笔记标注表结构

### 方法二：手动执行SQL文件

#### 步骤1：启动MySQL服务
```bash
# Windows
net start mysql

# Linux
sudo systemctl start mysql

# Mac
brew services start mysql
```

#### 步骤2：执行SQL文件

**方式A：使用整合的SQL文件（仅表结构，不包含数据）**
```bash
mysql -u root -p123456 < complete_database.sql
```
⚠️ **注意**：此文件只包含表结构，不包含测试数据。如需完整数据，请使用方式B或初始化脚本。

**方式B：按顺序执行分片SQL文件（推荐，包含完整数据）**
```bash
# 进入项目根目录
cd Webstorm-experiment

# 执行所有SQL文件（按顺序）
mysql -u root -p123456 < back/src/main/resources/sql/comprehensive_database_rebuild_part1.sql
mysql -u root -p123456 < back/src/main/resources/sql/comprehensive_database_rebuild_part2.sql
mysql -u root -p123456 < back/src/main/resources/sql/comprehensive_database_rebuild_part3.sql
mysql -u root -p123456 < back/src/main/resources/sql/comprehensive_database_rebuild_part4.sql
mysql -u root -p123456 < intelligent_features_database.sql
mysql -u root -p123456 < class_summary_table.sql
mysql -u root -p123456 < student_notes_annotations.sql
```

**方式C：使用简化数据库（快速测试）**
```bash
mysql -u root -p123456 < simple_init.sql
```

#### 步骤3：验证数据库创建
```sql
-- 登录MySQL
mysql -u root -p123456

-- 验证数据库
USE web_experiment;
SHOW TABLES;

-- 检查数据
SELECT COUNT(*) as total_users FROM user;
SELECT role, COUNT(*) as count FROM user GROUP BY role;
SELECT COUNT(*) as total_courses FROM course;
```

### 数据库配置检查

确保 `back/src/main/resources/application.yml` 中的配置正确：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/web_experiment?useUnicode=true&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123456  # 根据实际情况修改
    driver-class-name: com.mysql.cj.jdbc.Driver
```

---

## 🚀 后端服务启动

### 方法一：使用Maven命令（推荐）

```bash
# 1. 进入后端目录
cd Webstorm-experiment/back

# 2. 启动Spring Boot应用
mvn spring-boot:run
```

**启动成功标志**：
- 控制台显示：`Started WebExperimentApplication in X.XXX seconds`
- 后端服务运行在：`http://localhost:8080`

### 方法二：使用IDE启动

#### IntelliJ IDEA
1. 打开项目：`File` → `Open` → 选择 `Webstorm-experiment` 目录
2. 等待Maven依赖下载完成
3. 找到主类：`com.experiment.WebExperimentApplication`
4. 右键 → `Run 'WebExperimentApplication'`

#### Eclipse
1. 导入项目：`File` → `Import` → `Existing Maven Projects`
2. 选择 `back` 目录下的 `pom.xml`
3. 找到主类并运行

### 方法三：打包后运行

```bash
# 1. 进入后端目录
cd Webstorm-experiment/back

# 2. 打包项目
mvn clean package

# 3. 运行jar包
java -jar target/web-experiment-0.0.1-SNAPSHOT.jar
```

### 验证后端启动

启动成功后，可以访问：
- **健康检查**: http://localhost:8080/actuator/health
- **API测试**: http://localhost:8080/api/user/login (POST请求)

---

## 💻 前端服务启动

### 步骤1：安装依赖（首次运行）

```bash
# 进入前端目录
cd Webstorm-experiment/front(2)/front(1)

# 安装依赖
npm install
```

**注意**：如果安装失败，可以尝试：
```bash
# 清除缓存
npm cache clean --force

# 使用国内镜像
npm install --registry=https://registry.npmmirror.com
```

### 步骤2：启动开发服务器

```bash
# 启动Vite开发服务器
npm run dev
```

**启动成功标志**：
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

### 步骤3：构建生产版本（可选）

```bash
# 构建生产版本
npm run build

# 预览生产版本
npm run preview
```

---

## 🌐 系统访问与测试

### 访问地址
- **前端地址**: http://localhost:5173
- **后端API**: http://localhost:8080

### 测试账号

#### 管理员账号
- **用户名**: `admin`
- **密码**: `123456`
- **权限**: 系统管理、用户管理、数据统计

#### 教师账号
- **用户名**: `teacher1` 到 `teacher15`
- **密码**: `123456`
- **权限**: 课程管理、学生管理、考试创建、数据分析

**推荐测试账号**：
- `teacher1` - 张教授，教授数据结构、计算机网络、Linux系统

#### 学生账号
- **用户名**: `student1` 到 `student100`
- **密码**: `123456`
- **权限**: 在线学习、文档标注、练习考试、查看成绩

**推荐测试账号**：
- `student1` - 李同学，已选多门课程
- `student17` - 用于文档标注功能测试

### 快速测试流程

1. **登录系统**
   - 访问 http://localhost:5173
   - 使用 `student1` / `123456` 登录

2. **查看课程**
   - 进入"在线学习"页面
   - 查看已选课程列表

3. **文档标注测试**
   - 选择课程 → 查看资源 → 点击"预览标注"
   - 选择文本进行高亮、下划线、批注等操作

4. **练习测试**
   - 进入"练习"页面
   - 选择练习并完成
   - 查看AI分析结果

---

## 🎯 核心功能说明

### 1. 文档预览与标注功能

#### 功能特点
- **多种标注工具**: 高亮标记、下划线、文字批注、便签标记
- **智能交互**: 文本选择标注、位置精确保存、实时预览
- **现代化界面**: 双面板设计、缩放控制、响应式布局

#### 使用方法
1. 登录学生账号
2. 进入"在线学习" → 选择课程 → 查看资源
3. 点击"预览标注"按钮
4. 选择文本或点击工具栏进行标注
5. 在右侧面板管理所有标注

#### 数据库表
- `student_annotation` - 存储标注数据
- `student_note` - 存储笔记数据

### 2. AI智能分析功能

#### 功能特点
- **快速响应**: 1-3秒内显示基础结果
- **异步AI分析**: 后台生成详细分析，不阻塞界面
- **实时更新**: AI分析完成后自动更新界面

#### 性能优化
- **优化前**: 响应时间10-30秒
- **优化后**: 基础结果1-3秒，详细分析逐步显示

#### 使用说明
1. 提交练习后立即看到基础结果（分数、正确率）
2. 系统自动在后台生成AI详细分析
3. 可以手动触发单道题的AI分析
4. 状态显示：🟡 AI分析中、🟢 AI分析完成

### 3. 智能学习路径

#### 功能模块
- **知识点管理**: 知识点定义、依赖关系
- **学习路径模板**: 预设学习路径
- **个性化路径**: 根据学生能力定制学习计划
- **学习能力评估**: 学习速度、保持率、学习风格等

#### 相关数据表
- `knowledge_point` - 知识点表
- `knowledge_dependency` - 知识点依赖关系
- `learning_path_template` - 学习路径模板
- `personalized_learning_path` - 个性化学习路径
- `learning_ability_assessment` - 学习能力评估

### 4. 情绪感知学习助手

#### 功能特点
- **情绪检测**: 文本、面部、语音、行为多维度检测
- **学习状态监控**: 专注度、参与度、疲劳度、学习效率
- **智能干预**: 根据情绪状态提供学习建议
- **学习建议配置**: 可配置的情绪-建议映射规则

#### 相关数据表
- `emotion_detection_record` - 情绪检测记录
- `learning_status_monitor` - 学习状态监控
- `emotion_intervention_record` - 情绪干预记录
- `learning_suggestion_config` - 学习建议配置

### 5. 智能同伴学习

#### 功能模块
- **学习伙伴匹配**: 基于学习能力、兴趣匹配学习伙伴
- **协作学习会话**: 支持多种协作学习类型
- **学习小组**: 创建和管理学习小组
- **互动记录**: 记录协作学习互动数据

#### 相关数据表
- `learning_companion_matching` - 学习伙伴匹配
- `collaborative_learning_session` - 协作学习会话
- `collaborative_session_participant` - 会话参与者
- `collaborative_interaction_record` - 互动记录
- `study_group` - 学习小组
- `study_group_member` - 小组成员

### 6. 课堂重点整理

#### 功能特点
- **录音转录**: 支持音频文件上传和文本转录
- **AI生成摘要**: 自动生成课堂重点整理
- **教师编辑**: 支持教师编辑和发布
- **关键词标签**: 自动提取关键词

#### 相关数据表
- `class_summary` - 课堂重点整理表

### 7. 数据驾驶舱

#### 教师端功能
- **学生个人驾驶舱**: 学习概况、趋势分析、情绪状态
- **知识点掌握雷达图**: 多维度知识点掌握情况
- **学习时间分布**: 24小时学习活跃度分析
- **学习行为分析**: 专注度、坚持度、互动性、创新性评分
- **作业完成详情**: 完成率可视化、详细统计
- **考试成绩趋势**: SVG矢量图趋势线

#### 管理员端功能
- **系统概览**: 用户统计、课程统计、活跃度统计
- **数据可视化**: 多种图表展示

---

## ❓ 常见问题解决

### 1. MySQL连接失败

**错误信息**: `Communications link failure` 或 `Access denied`

**解决方案**:
```bash
# 1. 检查MySQL服务是否启动
net start mysql  # Windows
sudo systemctl status mysql  # Linux

# 2. 检查数据库配置
# 确认 application.yml 中的用户名和密码正确

# 3. 测试MySQL连接
mysql -u root -p123456 -e "SELECT 1;"

# 4. 检查端口是否被占用
netstat -ano | findstr :3306  # Windows
lsof -i :3306  # Linux/Mac
```

### 2. 端口被占用

**错误信息**: `Port 8080 is already in use`

**解决方案**:
```bash
# Windows: 查找并杀死进程
netstat -ano | findstr :8080
taskkill /PID <进程ID> /F

# Linux/Mac: 查找并杀死进程
lsof -i :8080
kill -9 <进程ID>

# 或者修改端口
# 编辑 back/src/main/resources/application.yml
server:
  port: 8081  # 改为其他端口
```

### 3. Maven依赖下载失败

**错误信息**: `Could not resolve dependencies`

**解决方案**:
```bash
# 1. 清除Maven缓存
mvn clean

# 2. 强制更新依赖
mvn clean install -U

# 3. 配置国内镜像（见环境配置部分）

# 4. 检查网络连接
ping repo.maven.apache.org
```

### 4. 前端依赖安装失败

**错误信息**: `npm ERR!` 相关错误

**解决方案**:
```bash
# 1. 清除npm缓存
npm cache clean --force

# 2. 删除node_modules和package-lock.json
rm -rf node_modules package-lock.json

# 3. 重新安装
npm install

# 4. 使用国内镜像
npm install --registry=https://registry.npmmirror.com

# 或安装cnpm
npm install -g cnpm --registry=https://registry.npmmirror.com
cnpm install
```

### 5. 数据库字符集问题

**错误信息**: 中文显示乱码

**解决方案**:
```sql
-- 检查数据库字符集
SHOW CREATE DATABASE web_experiment;

-- 修改数据库字符集
ALTER DATABASE web_experiment CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 检查表字符集
SHOW CREATE TABLE user;

-- 修改表字符集
ALTER TABLE user CONVERT TO CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 6. 前端页面空白

**问题**: 页面加载但显示空白

**解决方案**:
1. 打开浏览器开发者工具（F12）
2. 查看Console标签页是否有错误
3. 查看Network标签页，确认API请求是否成功
4. 确认后端服务已启动（http://localhost:8080）
5. 检查 `vite.config.js` 中的代理配置
6. 清除浏览器缓存并刷新（Ctrl+F5）

### 7. 后端启动失败

**错误信息**: `Application run failed`

**解决方案**:
1. 检查Java版本：`java -version` (需要21+)
2. 检查数据库连接配置
3. 查看完整错误日志，通常在控制台最后几行
4. 确认数据库已创建：`SHOW DATABASES;`
5. 检查端口是否被占用

### 8. 文档标注功能不显示

**问题**: 标注创建后不显示

**解决方案**:
1. 检查浏览器控制台是否有JavaScript错误
2. 确认标注数据已保存到数据库
3. 检查资源文件是否存在
4. 清除浏览器缓存
5. 检查标注位置数据格式是否正确

---

## 📝 一键启动脚本

### Windows一键启动（start_all.bat）

创建 `start_all.bat` 文件：

```batch
@echo off
echo ========================================
echo 启动智能学习管理系统
echo ========================================

echo [1/4] 启动MySQL服务...
net start mysql
if %errorlevel% neq 0 (
    echo MySQL服务启动失败，请手动启动
    pause
)

echo [2/4] 启动后端服务...
cd /d %~dp0back
start "SpringBoot Backend" cmd /k "mvn spring-boot:run"

echo [3/4] 等待后端启动（15秒）...
timeout /t 15 /nobreak > nul

echo [4/4] 启动前端服务...
cd /d %~dp0front(2)/front(1)
start "Vue Frontend" cmd /k "npm run dev"

echo.
echo ========================================
echo 启动完成！
echo.
echo 前端地址: http://localhost:5173
echo 后端地址: http://localhost:8080
echo.
echo 测试账号:
echo   管理员: admin / 123456
echo   教师: teacher1 / 123456
echo   学生: student1 / 123456
echo ========================================
pause
```

### Linux/Mac一键启动（start_all.sh）

创建 `start_all.sh` 文件：

```bash
#!/bin/bash
echo "========================================"
echo "启动智能学习管理系统"
echo "========================================"

echo "[1/4] 启动MySQL服务..."
sudo systemctl start mysql || brew services start mysql

echo "[2/4] 启动后端服务..."
cd "$(dirname "$0")/back"
mvn spring-boot:run &
BACKEND_PID=$!

echo "[3/4] 等待后端启动（15秒）..."
sleep 15

echo "[4/4] 启动前端服务..."
cd "../front(2)/front(1)"
npm run dev &
FRONTEND_PID=$!

echo ""
echo "========================================"
echo "启动完成！"
echo ""
echo "前端地址: http://localhost:5173"
echo "后端地址: http://localhost:8080"
echo ""
echo "测试账号:"
echo "  管理员: admin / 123456"
echo "  教师: teacher1 / 123456"
echo "  学生: student1 / 123456"
echo "========================================"
echo "按Ctrl+C停止所有服务"
wait
```

**使用前设置执行权限**:
```bash
chmod +x start_all.sh
```

---

## 📊 数据库表结构说明

### 核心业务表
- `user` - 用户表（管理员、教师、学生）
- `course` - 课程表
- `chapter` - 章节表
- `lesson` - 课时表
- `student_course` - 学生选课表
- `course_resource` - 课程资源表
- `exam` - 考试表
- `question` - 题目表
- `student_exam` - 学生考试记录表
- `practice` - 练习表
- `student_progress` - 学生学习进度表

### 智能功能表
- `knowledge_point` - 知识点表
- `learning_path_template` - 学习路径模板
- `personalized_learning_path` - 个性化学习路径
- `emotion_detection_record` - 情绪检测记录
- `learning_companion_matching` - 学习伙伴匹配
- `collaborative_learning_session` - 协作学习会话
- `study_group` - 学习小组

### 功能扩展表
- `class_summary` - 课堂重点整理表
- `student_annotation` - 学生标注表
- `student_note` - 学生笔记表

**完整表结构请参考**: `complete_database.sql`

---

## 🔄 完整运行流程

### 首次运行步骤

1. **环境准备** (5分钟)
   ```bash
   # 检查所有必需软件
   java -version
   mvn -version
   mysql --version
   node -v
   npm -v
   ```

2. **数据库初始化** (2-5分钟)
   ```bash
   # Windows: 双击运行
   init_database.bat
   
   # 或手动执行
   mysql -u root -p123456 < back/src/main/resources/sql/comprehensive_database_rebuild_part1.sql
   mysql -u root -p123456 < back/src/main/resources/sql/comprehensive_database_rebuild_part2.sql
   mysql -u root -p123456 < back/src/main/resources/sql/comprehensive_database_rebuild_part3.sql
   mysql -u root -p123456 < back/src/main/resources/sql/comprehensive_database_rebuild_part4.sql
   mysql -u root -p123456 < intelligent_features_database.sql
   mysql -u root -p123456 < class_summary_table.sql
   mysql -u root -p123456 < student_notes_annotations.sql
   ```

3. **启动后端服务** (1-2分钟)
   ```bash
   cd Webstorm-experiment/back
   mvn spring-boot:run
   ```
   等待看到: `Started WebExperimentApplication`

4. **启动前端服务** (30秒-1分钟)
   ```bash
   # 新开一个终端窗口
   cd Webstorm-experiment/front(2)/front(1)
   npm install  # 首次运行需要
   npm run dev
   ```
   等待看到: `Local: http://localhost:5173/`

5. **访问系统**
   - 打开浏览器访问: http://localhost:5173
   - 使用测试账号登录
   - 开始使用系统

### 日常运行步骤

1. **启动MySQL** (如果未自动启动)
   ```bash
   net start mysql  # Windows
   ```

2. **启动后端**
   ```bash
   cd Webstorm-experiment/back
   mvn spring-boot:run
   ```

3. **启动前端**
   ```bash
   cd Webstorm-experiment/front(2)/front(1)
   npm run dev
   ```

4. **访问系统**
   - 浏览器访问: http://localhost:5173

---

## 📚 相关文档

- **数据库设计**: `complete_database.sql` - 完整表结构
- **API文档**: `back/README.md` - 后端API说明
- **前端文档**: `front(2)/front(1)/README.md` - 前端开发说明
- **文档标注指南**: `DOCUMENT_ANNOTATION_GUIDE.md` - 标注功能详细说明
- **数据驾驶舱**: `DASHBOARD_FEATURES.md` - 数据分析功能说明
- **AI优化方案**: `AI_ANALYSIS_OPTIMIZATION.md` - AI分析性能优化

---

## 🆘 获取帮助

如果遇到问题：

1. **查看本文档** - 常见问题部分
2. **检查日志** - 后端日志在控制台，前端日志在浏览器控制台
3. **验证环境** - 确保所有软件版本符合要求
4. **数据库检查** - 确认数据库已正确初始化
5. **网络检查** - 确认前后端服务都在运行

---

## ✅ 快速检查清单

运行前请确认：

- [ ] Java 21+ 已安装
- [ ] Maven 3.6+ 已安装
- [ ] MySQL 8.0+ 已安装并启动
- [ ] Node.js 16+ 已安装
- [ ] 数据库已初始化（执行了SQL文件）
- [ ] 后端服务已启动（http://localhost:8080可访问）
- [ ] 前端服务已启动（http://localhost:5173可访问）
- [ ] 浏览器已打开并访问前端地址

---

**祝您使用愉快！** 🎉

如有问题，请参考本文档的"常见问题解决"部分，或查看相关功能文档。
