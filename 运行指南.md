# 智能学习管理系统 - 快速运行指南

> 📖 **完整文档**: 请查看 [完整使用指南.md](./完整使用指南.md) 获取详细说明

## 📋 快速开始

### 一键运行（推荐）

**Windows系统**:
```bash
# 1. 初始化数据库
双击运行 init_database.bat

# 2. 启动后端（新终端）
cd back
mvn spring-boot:run

# 3. 启动前端（新终端）
cd front(2)/front(1)
npm install  # 首次运行
npm run dev
```

**访问系统**: http://localhost:5173

### 测试账号
- 管理员: `admin` / `123456`
- 教师: `teacher1` / `123456`
- 学生: `student1` / `123456`

---

## 📋 详细步骤

---

## 🔧 环境要求

### 必需软件
- **Java**: JDK 21 或更高版本
- **Maven**: 3.6+ 
- **MySQL**: 8.0+ 
- **Node.js**: 16+ 
- **npm**: 8+ (通常随Node.js一起安装)

### 检查环境
```bash
# 检查Java版本
java -version

# 检查Maven版本
mvn -version

# 检查MySQL版本
mysql --version

# 检查Node.js版本
node -v

# 检查npm版本
npm -v
```

---

## 🗄️ 数据库初始化

### 方法一：使用批处理脚本（推荐）

**Windows系统：**
```bash
# 方式1：完整数据库（包含所有测试数据）
双击运行 init_database.bat

# 方式2：简化数据库（基础数据）
双击运行 init_database_simple.bat
```

**Linux/Mac系统：**
```bash
# 确保MySQL服务已启动
sudo systemctl start mysql  # Linux
brew services start mysql    # Mac

# 执行初始化脚本
chmod +x init_database.sh
./init_database.sh
```

### 方法二：手动执行SQL文件

1. **启动MySQL服务**
   ```bash
   # Windows
   net start mysql
   
   # Linux
   sudo systemctl start mysql
   
   # Mac
   brew services start mysql
   ```

2. **登录MySQL**
   ```bash
   mysql -u root -p123456
   ```

3. **执行SQL文件**
   ```bash
   # 方式1：使用整合的SQL文件（推荐）
   mysql -u root -p123456 < complete_database.sql
   
   # 方式2：按顺序执行分片SQL文件
   mysql -u root -p123456 < back/src/main/resources/sql/comprehensive_database_rebuild_part1.sql
   mysql -u root -p123456 < back/src/main/resources/sql/comprehensive_database_rebuild_part2.sql
   mysql -u root -p123456 < back/src/main/resources/sql/comprehensive_database_rebuild_part3.sql
   mysql -u root -p123456 < back/src/main/resources/sql/comprehensive_database_rebuild_part4.sql
   mysql -u root -p123456 < intelligent_features_database.sql
   mysql -u root -p123456 < class_summary_table.sql
   mysql -u root -p123456 < student_notes_annotations.sql
   ```

4. **验证数据库创建**
   ```sql
   USE web_experiment;
   SHOW TABLES;
   SELECT COUNT(*) FROM user;
   ```

### 数据库配置

确保 `back/src/main/resources/application.yml` 中的数据库配置正确：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/web_experiment?useUnicode=true&characterEncoding=UTF-8&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 123456  # 根据实际情况修改
    driver-class-name: com.mysql.cj.jdbc.Driver
```

---

## 🚀 后端启动

### 方法一：使用Maven命令（推荐）

```bash
# 进入后端目录
cd Webstorm-experiment/back

# 启动Spring Boot应用
mvn spring-boot:run
```

### 方法二：使用IDE

1. 使用 IntelliJ IDEA 或 Eclipse 打开项目
2. 找到主类：`com.experiment.WebExperimentApplication`
3. 右键运行 `Run 'WebExperimentApplication'`

### 方法三：打包后运行

```bash
# 进入后端目录
cd Webstorm-experiment/back

# 打包项目
mvn clean package

# 运行jar包
java -jar target/web-experiment-0.0.1-SNAPSHOT.jar
```

### 验证后端启动

后端启动成功后，访问以下URL验证：
- 健康检查：http://localhost:8080/actuator/health
- API文档：http://localhost:8080/swagger-ui.html（如果配置了Swagger）

---

## 💻 前端启动

### 安装依赖

```bash
# 进入前端目录
cd Webstorm-experiment/front(2)/front(1)

# 安装依赖（首次运行需要）
npm install
```

### 启动开发服务器

```bash
# 启动Vite开发服务器
npm run dev
```

### 构建生产版本

```bash
# 构建生产版本
npm run build

# 预览生产版本
npm run preview
```

### 验证前端启动

前端启动成功后，控制台会显示：
```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

---

## 🌐 访问系统

### 访问地址
- **前端地址**: http://localhost:5173
- **后端API**: http://localhost:8080

### 浏览器要求
- Chrome 90+
- Firefox 88+
- Edge 90+
- Safari 14+

---

## 👤 测试账号

### 管理员账号
- **用户名**: `admin`
- **密码**: `123456`
- **角色**: 管理员

### 教师账号
- **用户名**: `teacher1` 到 `teacher15`
- **密码**: `123456`
- **角色**: 教师

### 学生账号
- **用户名**: `student1` 到 `student100`
- **密码**: `123456`
- **角色**: 学生

**常用测试账号：**
- `student17` - 用于文档标注功能测试
- `teacher1` - 张教授，教授数据结构、计算机网络、Linux系统

---

## ❓ 常见问题

### 1. MySQL连接失败

**问题**: `Communications link failure`

**解决方案**:
- 检查MySQL服务是否启动
- 确认数据库用户名和密码正确
- 检查端口3306是否被占用
- 确认防火墙设置允许MySQL连接

### 2. 端口被占用

**问题**: `Port 8080 is already in use`

**解决方案**:
```bash
# Windows: 查找占用8080端口的进程
netstat -ano | findstr :8080

# Linux/Mac: 查找占用8080端口的进程
lsof -i :8080

# 杀死进程（Windows）
taskkill /PID <进程ID> /F

# 杀死进程（Linux/Mac）
kill -9 <进程ID>
```

或者修改 `application.yml` 中的端口：
```yaml
server:
  port: 8081  # 改为其他端口
```

### 3. 前端依赖安装失败

**问题**: `npm install` 失败

**解决方案**:
```bash
# 清除npm缓存
npm cache clean --force

# 删除node_modules和package-lock.json
rm -rf node_modules package-lock.json

# 重新安装
npm install

# 如果还是失败，尝试使用cnpm（国内镜像）
npm install -g cnpm --registry=https://registry.npmmirror.com
cnpm install
```

### 4. Maven依赖下载失败

**问题**: Maven下载依赖超时

**解决方案**:
- 检查网络连接
- 配置Maven国内镜像（阿里云）
- 在 `pom.xml` 所在目录执行：`mvn clean install -U`

### 5. 数据库字符集问题

**问题**: 中文显示乱码

**解决方案**:
确保数据库和表的字符集为 `utf8mb4`：
```sql
ALTER DATABASE web_experiment CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
```

### 6. 前端页面空白

**问题**: 页面加载但显示空白

**解决方案**:
- 检查浏览器控制台是否有错误
- 确认后端API服务已启动
- 检查 `vite.config.js` 中的代理配置
- 清除浏览器缓存并刷新

---

## 📝 快速启动脚本

### Windows一键启动（start_all.bat）

创建 `start_all.bat` 文件：

```batch
@echo off
echo ========================================
echo 启动智能学习管理系统
echo ========================================

echo 1. 启动MySQL服务...
net start mysql

echo 2. 启动后端服务...
start "SpringBoot Backend" cmd /k "cd back && mvn spring-boot:run"

echo 3. 等待后端启动（10秒）...
timeout /t 10 /nobreak > nul

echo 4. 启动前端服务...
start "Vue Frontend" cmd /k "cd front(2)/front(1) && npm run dev"

echo.
echo ========================================
echo 启动完成！
echo 前端地址: http://localhost:5173
echo 后端地址: http://localhost:8080
echo ========================================
pause
```

### Linux/Mac一键启动（start_all.sh）

创建 `start_all.sh` 文件：

```bash
#!/bin/bash
echo "========================================"
echo "启动智能学习管理系统"
echo "========================================"

echo "1. 启动MySQL服务..."
sudo systemctl start mysql

echo "2. 启动后端服务..."
cd back
mvn spring-boot:run &
BACKEND_PID=$!

echo "3. 等待后端启动（10秒）..."
sleep 10

echo "4. 启动前端服务..."
cd ../front(2)/front(1)
npm run dev &
FRONTEND_PID=$!

echo ""
echo "========================================"
echo "启动完成！"
echo "前端地址: http://localhost:5173"
echo "后端地址: http://localhost:8080"
echo "========================================"
echo "按Ctrl+C停止所有服务"
wait
```

---

## 📚 相关文档

- [数据库设计文档](./DOCUMENT_ANNOTATION_GUIDE.md)
- [API文档](./back/README.md)
- [前端开发文档](./front(2)/front(1)/README.md)
- [智能功能说明](./AI_ANALYSIS_OPTIMIZATION.md)

---

## 🆘 获取帮助

如果遇到问题，请：
1. 查看本文档的常见问题部分
2. 检查项目README文件
3. 查看日志文件（`back/logs/` 目录）
4. 提交Issue到项目仓库

---

## 📚 完整文档

**详细说明请查看**: [完整使用指南.md](./完整使用指南.md)

该文档包含：
- ✅ 完整的环境配置说明
- ✅ 详细的数据库初始化步骤
- ✅ 所有核心功能说明
- ✅ 完整的故障排除指南
- ✅ 数据库表结构说明
- ✅ 一键启动脚本

---

**祝您使用愉快！** 🎉
